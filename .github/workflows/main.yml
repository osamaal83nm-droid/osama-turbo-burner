name: Osama Turbo Burner Core
on:
  workflow_dispatch:
jobs:
  run_core:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 سحب الكود
        uses: actions/checkout@v2
      - name: 🐍 تجهيز بيئة بايثون
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'
      - name: 📦 تثبيت مكتبات لقمة العيش
        run: |
          python -m pip install --upgrade pip
          pip install requests pyairtable
      - name: 🚀 إطلاق المهمة
        env:
          AIRTABLE_API_KEY: ${{ secrets.AIRTABLE_API_KEY }}
        run: python main.py
        
